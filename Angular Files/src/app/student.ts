

/* where the data will be stored(used to store data from the Get and post method) for http call*/
export class Student {
    id: string;
    firstName: string;
    lastName: string;
    emailId: string;
    streetAddress: string;
    city: string;
    state: string;
    zipcode: string;
    date: string;
}

